
jetty:
  staticResourceBase: classpath:static/
  connectors:
  -
    port: 8001

mvc:
  templateBase: "classpath:"

#log:
#  level: debug
#  loggers:
#    org.apache.shiro:
#      level: debug
#    org.apache.jetty:
#      level: debug

jdbc:
  MySqlTest:
    url:  jdbc:mysql://localhost:3306/uploadertestdb
    driverClassName: com.mysql.jdbc.Driver
    username: devuser
    password: password
    validationQuery: "SELECT 1"
    testOnBorrow: true
    testWhileIdle: true

jdbctest:
  quoteIdentifiers: true

shiroweb:
  urls:
    "/admin" : roles[admin]
    "/users"   : authc
    "/login" : anon
    "/" : anon
    "/authorized" : anon








